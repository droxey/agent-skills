# Skills Directory

This directory contains all managed skills, organised by domain:

```
skills/
└── <domain>/
    └── <skill-id>.md
```

Skills are Markdown files with YAML frontmatter.  See the root README for the
full schema definition and contribution guide.
